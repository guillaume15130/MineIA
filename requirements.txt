# Core deep learning - PyTorch with CUDA 12.1 (RTX 4080 Ada Lovelace)
# Install with: pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu121
torch==2.3.1+cu121
torchvision==0.18.1+cu121

# RL framework
stable-baselines3[extra]==2.3.2
gymnasium==0.29.1
sb3-contrib==2.3.0

# MineRL environment (requires Java 8 - see scripts/setup_env.sh)
# Pinned to 1.0.x which targets MC 1.16+ and is closer to VPT data distribution
minerl==1.0.2

# VPT model dependencies (OpenAI's foundation model for Minecraft)
attrs>=21.4.0
opencv-python==4.10.0.84
pillow>=10.0.0

# Logging / experiment tracking
wandb==0.17.0
tensorboard==2.16.2

# Config / utils
pyyaml==6.0.1
hydra-core==1.3.2
tqdm==4.66.4
numpy==1.26.4

# Dev / quality
pytest==8.2.2
ruff==0.5.0
